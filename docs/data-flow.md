# Data flow

The planned flow is browser authentication, direct encrypted document upload, asynchronous
extraction and PHI screening, normalized parsing, transactional persistence, trusted
retrieval, schema-validated agent output, and human-reviewed action application.

No runtime data flow is implemented in Phase 0.
