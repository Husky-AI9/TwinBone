# Synthetic fixtures

Only plainly synthetic, non-identifiable document fixtures may be added here. The custom BMD
parser is pre-existing work and must retain explicit provenance if imported in Phase 3.
